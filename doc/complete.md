@function can-animate-complete
@parent can-animate.api

@signature `can-animate-complete="{PROPERTY}"`

@param {Function} [PROPERTY] Name of the scope function

@body

## Use

Specify a function that will be called when the animation completes.

```
<div can-animation-complete="{animationCompleted}"></div>
```
